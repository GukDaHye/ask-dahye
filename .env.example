OPENAI_API_KEY=

# GitHub MCP 연동 (선택). 둘 다 비어 있으면 코드 질문도 기존 RAG 경로로 처리된다.
#
# 대상은 이 저장소 하나다. "이 챗봇은 어떻게 만들어졌나"류 질문에 실제 소스를 근거로
# 답하는 것이 목적이므로 다중 리포는 범위가 아니다.
#
# 토큰은 fine-grained PAT으로 만들고 권한을 최소로 준다.
#   Repository access : 이 저장소 하나만
#   Permissions       : Contents = Read-only
GITHUB_TOKEN=
GITHUB_REPO=GukDaHye/ask-dahye
